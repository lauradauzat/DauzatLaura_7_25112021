const express = require("express");
const router = express.Router();

const signalCtrl = require ('../controllers/Signalements'); 

//router.post('/:id', signalCtrl.addSignal);


module.exports = router;
